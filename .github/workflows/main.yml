name: Main MonoGame build workflow

on: [push, pull_request]

jobs:
  build-and-deploy:
    strategy:
      matrix:
        include:
          - os: windows
            platform: windows
            shell: cmd
          - os: macos
            platform: macos
            shell: bash
          - os: ubuntu-latest
            platform: linux
            shell: bash
            filter: --where="Category != Audio"
    runs-on: ${{ matrix.os }}
    steps:
      - uses: ./.github/workflows/checkdependencies.yml
        name: Check Dependencies
        with:
          os: ${{ matrix.os }}
      - uses: ./.github/workflows/build.yml
        name: Build
        with:
          os: ${{ matrix.os }}
          platform: ${{ matrix.platform }}
          shell: ${{ matrix.shell }}
          filter: ${{ matrix.filter }}

      - uses: ./.github/workflows/tests.yml
        name: Tests
        with:
          os: ${{ matrix.os }}
          platform: ${{ matrix.platform }}
          shell: ${{ matrix.shell }}
          filter: ${{ matrix.filter }}

      - uses: ./.github/workflows/tests-graphics.yml
        name: Graphics Tests
        with:
          os: ${{ matrix.os }}
          platform: ${{ matrix.platform }}
          shell: ${{ matrix.shell }}
          filter: ${{ matrix.filter }}
        if: ${{ runner.environment != 'github-hosted'}}

      - uses: ./.github/workflows/deploy.yml
        name: Deploy
        with:
          os: ${{ matrix.os }}
        if: ${{ github.event_name == 'push' && success() }}